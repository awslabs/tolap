{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    // Imports name the file that exists on disk. Vite resolves and rewrites them
    // at build time, so the extension never reaches a browser.
    "allowImportingTsExtensions": true,
    // `node` for vite.config.ts, which reads process.env to build the CSP.
    "types": ["vitest/globals", "vite/client", "node"]
  },
  "include": ["src", "vite.config.ts"]
}
