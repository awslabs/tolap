[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tolap-core"
version = "2.0.0"
requires-python = ">=3.10"
description = "TOLAP SDK Core - Models, merge algorithm, signing, enforcement"
