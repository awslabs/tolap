kb metadata-filter verification — real OpenSearch 2.19 and Elasticsearch 7.10
Run: 2026-07-30T20:40:07Z
Account: <ACCOUNT_ID> (isengard sandbox)  Region: us-east-1
Command: python3 verbose_kb_search_log.py --state <state>

This run is AFTER the .keyword fail-open fix.

kb metadata-filter verification against real search engines
Region : us-east-1
  OpenSearch 2.19      provider=opensearch      search-tolap-opensearch-run2-d5blrvprm2nt37mpc5tblu4iya.us-east-1.es.amazonaws.com
  Elasticsearch 7.10   provider=elasticsearch   search-tolap-elasticsearch-run2-hj3aiuvmwi43ctbxsh62x3gmbu.us-east-1.es.amazonaws.com

Both renderers are marked `fromGrammar`: written from the documented DSL, never
confirmed by an engine. Every filter below was generated by the shipped renderer and
sent to the real service. Claims are checked as they print; a FAIL exits non-zero.

==============================================================================
OpenSearch 2.19: the kb filter pushdown against the real engine
==============================================================================
    unfiltered: 4 document(s)
        id=1 classification='public'
        id=2 classification='public'
        id=3 classification='secret'
        id=4 classification='secret'
    OK   the corpus holds BOTH classifications, so an exclusion test is not vacuous -- an index that never returns secret would make any filter look effective

--- deniedTags: [secret]
    policy: tagRules.deniedTags=[secret]
    filter sent: {"bool": {"must_not": [{"bool": {"should": [{"terms": {"classification": ["secret"]}}, {"terms": {"classification.keyword": ["secret"]}}], "minimum_should_match": 1}}]}}
    returned   : 2 document(s)
        id=1 classification='public'
        id=2 classification='public'
    OK   the engine ACCEPTED the generated filter document (no parse error)
    OK   no secret document survived -- the real engine applied our filter
    OK   public documents remain, so the filter excluded rather than matching nothing -- a query that returns zero hits parses fine and enforces nothing
    OK   fewer hits than unfiltered, so the reduction is the engine's work

--- allowedTags: [public]
    policy: tagRules.allowedTags=[public]
    filter sent: {"bool": {"filter": [{"bool": {"should": [{"terms": {"classification": ["public"]}}, {"terms": {"classification.keyword": ["public"]}}], "minimum_should_match": 1}}]}}
    returned   : 2 document(s)
    OK   only public documents are returned

--- both, ANDed
    policy: allowedTags=[public], deniedTags=[secret]
    filter sent: {"bool": {"filter": [{"bool": {"should": [{"terms": {"classification": ["public"]}}, {"terms": {"classification.keyword": ["public"]}}], "minimum_should_match": 1}}], "must_not": [{"bool": {"should": [{"terms": {"classification": ["secret"]}}, {"terms": {"classification.keyword": ["secret"]}}], "minimum_should_match": 1}}]}}
    returned   : 2 document(s)
    OK   the combined filter is accepted and returns only public

--- pushdown vs the normative post-pass
    policy: deniedTags=[secret]
    pushdown : ids=['1', '2']
    post-pass: ids=['1', '2']
    OK   the source filter and the shipped post-retrieval pass reach the SAME verdict -- this is the property that makes a pushdown safe rather than merely faster

--- NEGATIVE CONTROL: a malformed filter is refused
    policy: hand-written invalid DSL
    rejected: HTTP 400: {"error":{"root_cause":[{"type":"parsing_exception","reason":"[terms] query malformed, no start_obje
    OK   the engine rejects a malformed query, so 'accepted' above is a real signal

==============================================================================
Elasticsearch 7.10: the kb filter pushdown against the real engine
==============================================================================
    unfiltered: 4 document(s)
        id=1 classification='public'
        id=2 classification='public'
        id=3 classification='secret'
        id=4 classification='secret'
    OK   the corpus holds BOTH classifications, so an exclusion test is not vacuous -- an index that never returns secret would make any filter look effective

--- deniedTags: [secret]
    policy: tagRules.deniedTags=[secret]
    filter sent: {"bool": {"must_not": [{"bool": {"should": [{"terms": {"classification": ["secret"]}}, {"terms": {"classification.keyword": ["secret"]}}], "minimum_should_match": 1}}]}}
    returned   : 2 document(s)
        id=1 classification='public'
        id=2 classification='public'
    OK   the engine ACCEPTED the generated filter document (no parse error)
    OK   no secret document survived -- the real engine applied our filter
    OK   public documents remain, so the filter excluded rather than matching nothing -- a query that returns zero hits parses fine and enforces nothing
    OK   fewer hits than unfiltered, so the reduction is the engine's work

--- allowedTags: [public]
    policy: tagRules.allowedTags=[public]
    filter sent: {"bool": {"filter": [{"bool": {"should": [{"terms": {"classification": ["public"]}}, {"terms": {"classification.keyword": ["public"]}}], "minimum_should_match": 1}}]}}
    returned   : 2 document(s)
    OK   only public documents are returned

--- both, ANDed
    policy: allowedTags=[public], deniedTags=[secret]
    filter sent: {"bool": {"filter": [{"bool": {"should": [{"terms": {"classification": ["public"]}}, {"terms": {"classification.keyword": ["public"]}}], "minimum_should_match": 1}}], "must_not": [{"bool": {"should": [{"terms": {"classification": ["secret"]}}, {"terms": {"classification.keyword": ["secret"]}}], "minimum_should_match": 1}}]}}
    returned   : 2 document(s)
    OK   the combined filter is accepted and returns only public

--- pushdown vs the normative post-pass
    policy: deniedTags=[secret]
    pushdown : ids=['1', '2']
    post-pass: ids=['1', '2']
    OK   the source filter and the shipped post-retrieval pass reach the SAME verdict -- this is the property that makes a pushdown safe rather than merely faster

--- NEGATIVE CONTROL: a malformed filter is refused
    policy: hand-written invalid DSL
    rejected: HTTP 400: {"error":{"root_cause":[{"type":"parsing_exception","reason":"[terms] query malformed, no start_obje
    OK   the engine rejects a malformed query, so 'accepted' above is a real signal

==============================================================================
cross-engine: the two renderers emit the same document -- do both engines agree?
==============================================================================
    opensearch   : {"bool": {"must_not": [{"bool": {"minimum_should_match": 1, "should": [{"terms": {"classification": ["secret"]}}, {"terms": {"classification.keyword": ["secret"]}}]}}]}}
    elasticsearch: {"bool": {"must_not": [{"bool": {"minimum_should_match": 1, "should": [{"terms": {"classification": ["secret"]}}, {"terms": {"classification.keyword": ["secret"]}}]}}]}}
    OK   the renderers emit identical documents, which is why they share an implementation
    OK   and both engines reached the same verdict on it -- if the DSLs had drifted, one of these would have failed above and the shared implementation would need splitting

==============================================================================
All claims held against both live engines.
